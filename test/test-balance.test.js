const assert = require('assert');
describe('Get Balance Test', () => {
 it('should return Balance of 2', () => {
        assert(1 + 1, 2);
    });
 it('should return Balance of 9.7765', () => {
        assert((3 * 3)+0.7765, 9);
    });
});